{
  "$schema": "https://deno.land/x/denon@2.5.0/schema.json",
  "scripts": {
    "start": {
      "cmd": "deno run --no-check --allow-all --import-map imports.json mod.ts",
      "desc": "Start the bot"
    }
  },
  "watcher": {
    "skip": ["*/.git/*", "fileloader.ts"]
  }
}